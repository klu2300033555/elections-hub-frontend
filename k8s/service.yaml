apiVersion: v1
kind: Service
metadata:
  name: voting-frontend-service
spec:
  type: NodePort
  selector:
    app: voting-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
